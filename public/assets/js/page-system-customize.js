/**
 * Account Settings - Account
 */

'use strict';

document.addEventListener('DOMContentLoaded', function (e) {
  (function () {
    // let selectMac = document.getElementById('macList')
    // selectMac.onchange = () => {
    //   var selected = selectMac.options[selectMac.selectedIndex];
    //   $systemConfig['mac'] = selected.text;
    //   console.log($systemConfig['mac'])
    // }

    // let addModel = $('#addModel');
    // let offcanvas = $('#offcanvasEnd')
    // addModel.on( "click", function() {
    //   addModel.attr('data-bs-toggle', 'offcanvas');
    //   addModel.attr('data-bs-target', '#offcanvasEnd');
    //   addModel.attr('aria-controls', 'offcanvasEnd');
    //   if(addModel.attr('id') == 'addModel')
    //   {
        
    //   } 
    // } );

  })();
});
