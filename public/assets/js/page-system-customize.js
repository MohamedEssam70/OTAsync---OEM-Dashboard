/**
 * Account Settings - Account
 */

'use strict';

document.addEventListener('DOMContentLoaded', function (e) {
  (function () {
    // let selectMac = document.getElementById('macList')
    // selectMac.onchange = () => {
    //   var selected = selectMac.options[selectMac.selectedIndex];
    //   $systemConfig['mac'] = selected.text;
    //   console.log($systemConfig['mac'])
    // }
  })();
});
